We need to fix the RIGHT PANEL “Add Exercise” action so it always performs a true exercise addition, not a generic volume increase.

PROBLEM:
When the user taps “Add Exercise” on a day card in the right panel, the system currently interprets it too loosely and may respond by:
- increasing accessory sets
- increasing volume
- keeping primary structure unchanged

This is wrong.

If the UI says:
- Add Exercise

then the mutation must:
- add a new exercise row to that specific day/session
- chosen from the exercise library
- aligned with the session identity and program context

It must NOT silently turn into “add volume.”

==================================================
PRIMARY GOAL
==================================================

Make “Add Exercise” from the right panel a strict structural mutation:
- add one real exercise entity to the targeted day

==================================================
TASK 1 — TRACE RIGHT PANEL SESSION ACTION PAYLOAD
==================================================

Inspect the right-panel session action for:
- Add Exercise

Log exactly what is sent through the conversation route:
- raw text message
- metadata (dayIndex, source, interactionType)
- anything else attached

I want to confirm what the button is actually telling the backend.

==================================================
TASK 2 — TRACE INTENT + EXECUTION PLAN
==================================================

For the exact message generated by the right-panel Add Exercise action, trace:

- normalized intent family
- execution-planner action
- mutation type
- any subtype such as session_expansion / increase_volume / add

Find exactly where “add exercise” is being collapsed into a generic volume expansion concept.

==================================================
TASK 3 — ADD A STRICT ADD_EXERCISE PATH
==================================================

Add a strict execution path for UI-triggered add-exercise actions.

If interactionType === "session_action" and button === "Add Exercise"
OR message clearly means “add an exercise to Day X”

then the planner / edit interpretation must produce a mutation that means:

- ADD_EXERCISE_ROW
- target specific day/session
- choose a new exercise from the library
- insert a real exercise record

Do not let this route degrade into:
- increase sets
- increase reps
- increase generic volume

==================================================
TASK 4 — DISTINGUISH “ADD EXERCISE” FROM “INCREASE VOLUME”
==================================================

Create a clear separation between these intents:

A. ADD_EXERCISE
Examples:
- add an exercise to day 1
- add another movement
- add one more exercise here

B. INCREASE_VOLUME
Examples:
- add volume
- make this more challenging
- increase workload
- add more work

Only A should be triggered by the right-panel Add Exercise action.

==================================================
TASK 5 — LIBRARY-BACKED EXERCISE SELECTION
==================================================

When ADD_EXERCISE executes, the new exercise must come from the exercise library and fit:

- the day/session identity
- the slot needs of the session
- the sport/category
- current exercises already in the day
- equipment constraints
- fatigue / redundancy guardrails

Do NOT append a random exercise.
Do NOT add duplicate anchors unless intentionally allowed.

Prefer adding something that complements the session structure.

==================================================
TASK 6 — REQUIRE STRUCTURAL VERIFICATION
==================================================

For ADD_EXERCISE mutations, verification must confirm:

- exercise count in target day increased by 1
- new exercise row exists
- inserted exercise has a valid canonical name / ID

If exercise count did not increase:
- mutation failed
- do NOT report success
- do NOT say “Program Updated”

==================================================
TASK 7 — FIX FINAL RESPONSE TEXT
==================================================

If the action was Add Exercise, the final assistant response must explicitly state what was added.

Example:
- “Added Copenhagen Plank to Day 1 to support trunk stability.”
- “Added Lateral Bound to Day 1 to increase elastic power.”

Do NOT allow responses like:
- “adding volume”
- “expanding accessory sets”
- “primary structure unchanged”

for this specific UI action.

==================================================
TASK 8 — RIGHT PANEL UI CONTRACT
==================================================

The right panel button label “Add Exercise” is a promise.

That promise must now mean:
- add a new exercise row

If the backend cannot do that confidently, then:
- ask a clarification question
OR
- fail safely

But do not silently convert to another type of mutation.

==================================================
TASK 9 — TEST REQUIRED
==================================================

Test these exact cases:

1. Tap “Add Exercise” on Day 1 from right panel
Expected:
- one new exercise row added to Day 1
- final response names the new exercise

2. Tap “Add Exercise” on another day with different session identity
Expected:
- added exercise fits that session, not generic filler

3. Send chat message:
- “Add an exercise to Day 1”
Expected:
- same structural behavior as right-panel button

4. Send chat message:
- “Add volume to Day 1”
Expected:
- may increase sets/work, but does NOT need to add a new exercise row

This confirms the two intents are properly separated.

==================================================
TASK 10 — OUTPUT REQUIRED
==================================================

Return:
1. what payload the Add Exercise button was sending
2. where it was being misclassified
3. what strict mutation path now handles Add Exercise
4. how library-backed exercise selection works
5. how structural verification was added
6. which exact tests now pass

==================================================
FINAL STANDARD
==================================================

When the user taps “Add Exercise” in the right panel, TrainChat must add a real exercise from the library to that specific day.

It must never silently interpret that as only adding sets or generic volume.